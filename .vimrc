" Basic vim functionality, changes on upgrade (do not remove)
runtime! archlinux.vim

" do not load defaults if ~/.vimrc is missing
"let skip_defaults_vim=1

" Own configuration starts here
set number
syntax on
